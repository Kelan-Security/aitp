global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: kelan
    static_configs:
      - targets: ["kelan:3000"]
    metrics_path: /metrics

  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
